---
tags:
  - NLP
source: 《尚硅谷NLP》第4章
date: 2026-06-07
---

# 传统序列模型

> 核心问题：

---

## 4.1 RNN
### 概述
- 自然语言中，词语的顺序对于理解句子的含义至关重要，这是词向量中所不包含的信息
- RNN 会逐个读取句子中的词语，在每一步结合当前词和上下文信息。通过这种机制，RNN 能持续建模出句子的整体语义
- 但随着技术的发展，RNN 已被结构更灵活、计算效率更高效的 Transformer 模型所取代，但这是后话了
### 基础结构
- RNN 的核心结构是一个具有循环连接的隐藏层。以时间步为单位，依次处理输入序列中的每个 token
- 在每个时间步，RNN 接收当前 token 的向量和上一个时间步的隐藏状态（隐藏层的输出），计算新的隐藏状态，并传递给下一个时间步
- 其中隐藏层的计算公式为 $h_t=tanh(x_tW_x+h_{t-1}W_h+b)$
![[Pasted image 20260607200409.png]]
### 多层结构
- 为了捕捉更复杂的语言特征，可以将多个 RNN 层堆叠起来，让不同层学习不同层次的语义信息
- 核心假设：底层网络更容易捕捉局部模式（词组、短语），高层则能学习更抽象的语义信息（句子主题或语境）
- 多层 RNN 结构中，每一层的输出序列作为下一层的输入序列
### 双向结构
- 基础 RNN 在每个时间步只输出一个隐藏状态，该状态包含来自上文的信息，而无法利用当前词下文的信息
- 对于例如序列标注任务，模型需为每个 token 预测一个标签，如果只参考前文信息，往往难以做到准确的判断
- 而使用双向 RNN（Bidirectional RNN），模型可以在每个时间步同时利用前文和后文的信息，从而获得更全面的上下文表示，有助于提升序列标注等任务的预测效果。
![[Pasted image 20260607200218.png]]
### 多层+双向结构
- 多层结构和双向结构可以组合使用，每一层都是一个双向 RNN
### Pytorch API 使用
- 使用 `torch.nn.RNN` 模块用于构建 RNN
#### 参数

| **参数名**           | **类型**               | **说明**                                                          |
| ----------------- | -------------------- | --------------------------------------------------------------- |
| **input_size**    | int                  | 每个时间步输入特征的维度（词向量维度）                                             |
| **hidden_size**   | int                  | 隐藏状态的维度                                                         |
| **num_layers**    | int                  | RNN 层数，默认为 1                                                    |
| **nonlinearity**  | str                  | 激活函数，'tanh'（默认）或 'relu'                                         |
| **bias**          | bool                 | 是否使用偏置项，默认 True                                                 |
| **batch_first**   | bool                 | 输入张量是否是 (batch, seq, feature)，默认 False 表示 (seq, batch, feature) |
| **dropout**       | float                | 除最后一层外，其余层之间的 dropout 概率                                        |
| **bidirectional** | bool                 | 是否为双向 RNN，默认 False                                              |
| **device**        | torch. device or str | 模块的初始化设备，如 'cuda', 'cpu'                                        |
| **dtype**         | torch. dtype         | 模块初始化时的默认数据类型，如 torch. float 32                                 |

#### 输入输出
##### 输入
- **input**：输入序列，形状为(seq_len, batch_size, input_size)，如果 `batch_first=True`，则为 (batch_size, seq_len, input_size)
- **h_0**：可选，初始隐藏状态，形状为 (num_layers × num_directions, batch_size, hidden_size)

##### 输出
- **output**：RNN 层的输出，包含最后一层每个时间步的隐藏状态，形状为 (seq_len, batch_size, num_directions × hidden_size )，如果如果 `batch_first=True`，则为 (batch_size, seq_len, num_directions × hidden_size )
- **h_n**：最后一个时间步的隐藏状态，包含每一层的每个方向，形状为 (num_layers × num_directions, batch_size, hidden_size)

##### 示例
- **单层单向**
![[Pasted image 20260609215756.png]]
- **多层单向**
![[Pasted image 20260609220246.png]]
- **单层双向**
![[Pasted image 20260609220504.png]]
- **多层双向**
![[Pasted image 20260609220516.png]]

### 智能输入法案例
#### 介绍
- 本案例旨在实现一个用于手机输入法的**智能词语联想模型**。  
- 具体需求为：根据用户当前已输入的文本内容，预测下一个可能输入的词语，要求返回概率最高的 5 个候选词供用户选择。